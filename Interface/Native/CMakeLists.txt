cmake_minimum_required (VERSION 3.13)

set(NATIVE_INCLUDE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/include" PARENT_SCOPE)

file(GLOB NATIVE_HFILES "${CMAKE_CURRENT_SOURCE_DIR}/include/*.h")
file(GLOB NATIVE_SRCFILES "${CMAKE_CURRENT_SOURCE_DIR}/src/*.cpp")

set(NATIVE_H "${NATIVE_HFILES}" PARENT_SCOPE)
set(NATIVE_SRC "${NATIVE_SRCFILES}" PARENT_SCOPE)
